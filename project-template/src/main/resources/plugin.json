{
  "description":{
    "creator" : "${authorName}",
    "game" : "${gameName}",
    "version" : "${pluginVersion}",
    "toolVersion": "${toolVersion}"
  },
  "resources" : {
    "character" : "templates/sheet.html",
    "description" : "templates/plugin-description.html"
  }
}