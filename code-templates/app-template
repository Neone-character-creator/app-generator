import React from "react";
import { connect } from "react-redux";
%imports%

const Component = () => (
    function renderViews(){
        return(
            %views%
        )
    };
    <div id="%appName%-app">
        {renderViews()}
    </div>
);

module.exports = Component;