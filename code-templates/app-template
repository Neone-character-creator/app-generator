import React from "react";
import { connect } from "react-redux";
%imports%

function renderViews(){
        return(
            %views%
        )
    };

const Component = () => (
    <div id="%appName%-app">
        {renderViews()}
    </div>
);

module.exports = Component;