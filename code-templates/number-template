class Component extends React.Component {
    render() {
        return (
            <Textfield id="${componentIdentifier}" label="${this.props.label}" type="number" value={this.props.value} onChange={props.update} />
        );
    }

    componentShouldUpdate(nextProps){
        return this.props.value !== nextProps.value;
    }
}