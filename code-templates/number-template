class Component extends React.Component {
    render() {
        return (
            <Textfield id="${componentIdentifier}" label="${label}" type="number" value={this.props.value} onChange={this.props.update} />
        );
    }

    componentShouldUpdate(nextProps){
        return this.props.value !== nextProps.value;
    }
}