class Component extends React.Component {
    render(){
        return (<Grid id="${componentIdentifier}" container spacing={8} justify="space-around" direction={"${direction}" == "vertical" ? "column" : "row"}>
                    ${children}
                </Grid>);
    }

    shouldComponentUpdate(){
        return false;
    }
}