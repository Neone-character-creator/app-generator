const Component = () => (
    <Grid id="${componentName}-container" container spacing={8} justify="space-around" direction={"${direction}" == "vertical" ? "column" : "row"}>
        ${children}
    </Grid>
);