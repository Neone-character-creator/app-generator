const Child = (props) => {
    return (${childComponent})
};

const Component = (props) => (
    <List>
        {(props.value || []).map(i =>
            <ListItem>
                <Child value={i} context={props.context} />
            </ListItem>)
        }
    </List>
);