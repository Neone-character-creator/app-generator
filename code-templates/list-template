const Child = (props) => {
    return (${childComponent})
};

const Component = (props) => (
    <List>
        {(props.value || []).map((v, index) => {
            return (<ListItem>
                <Child value={v} index={index} />
            </ListItem>)
            })
        }
    </List>
);