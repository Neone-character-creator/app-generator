import React from "react";
import { connect } from "react-redux";
import Grid from "@material-ui/core/Grid";
${imports}

const Component = () => (
    <div id="${componentName}-view">
        ${children}
    </div>
);

export default Component;